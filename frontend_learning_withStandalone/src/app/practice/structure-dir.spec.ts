import { StructureDir } from './structure-dir';

describe('StructureDir', () => {
  it('should create an instance', () => {
    // const directive = new StructureDir();
    // expect(directive).toBeTruthy();
  });
});
